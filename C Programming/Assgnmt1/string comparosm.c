#include <stdio.h>
int main()
{

    char a[10,b[10];
    int i, len1, len2, status=1;

    printf("enter string1");
    scanf("%s,a");

    printf("enter string1";
    scanf("%s,b");
    for(len1=0;a[len1]!='\0';len1++);
    for(len2=0;a[len2]!='\0';len1++);

    if(len)
